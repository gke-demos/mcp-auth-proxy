apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Default namespace to apply to all resources in this overlay.
namespace: kagent

# Inherit from the base proxy installation and add the optional RemoteMCPServer resource
resources:
  - ../base
  - remotemcpserver.yaml
